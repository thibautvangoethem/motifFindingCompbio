# Simple bean cinfig class, the defaults can be adjusted here, but the argument will always come first
class Config:
    max_gapsize=0
    non_uniform_distribution=False
    backgroundvector=[1, 1, 1, 1]
    palindrome_enable=False
    palindrome=0.0
    batch=1